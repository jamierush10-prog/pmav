rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Basic baseline: only signed-in users can read/write. Tighten per collection as needed.
      allow read, write: if request.auth != null;
    }
  }
}
